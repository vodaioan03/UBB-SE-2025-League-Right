<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Duo.Views.Components.MultipleChoiceExercise"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Duo.Models.Exercises"
    mc:Ignorable="d">

    <StackPanel Orientation="Vertical" Spacing="6" HorizontalAlignment="Center" Margin="100">
        <TextBlock 
                Text="{x:Bind Question, Mode=OneWay}"
                HorizontalAlignment="Center" 
                FontSize="32"
                Margin="0, 0, 0,100"
                />
        <ItemsControl ItemsSource="{x:Bind Answers, Mode=OneWay}" Grid.Column="0" HorizontalAlignment="Center">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="models:MultipleChoiceAnswerModel">
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <Button Background="Transparent"
                                BorderThickness="1"
                                Padding="12"
                                Click="Option_Click"
                                Content="{x:Bind Answer}" 
                                Width="150" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Background="Transparent"
                HorizontalAlignment="Center"
                BorderThickness="5"
                Padding="12"  
                Click="Send_Click" 
                Width="150" Margin="40">Send</Button>
    </StackPanel>

</UserControl>
