<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Duo.Views.Components.FillInTheBlanksExercise"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Duo.Models.Exercises"
    xmlns:helpers="using:Duo.Views.Helpers"
    mc:Ignorable="d">

    <UserControl.Resources>
        <local:FillInTheBlanksExercise x:Key="FillInTheBlanksConverter"/>
    </UserControl.Resources>

    <StackPanel Orientation="Vertical" Spacing="6" HorizontalAlignment="Center" Margin="100">
        <TextBlock 
        Text="Fill in the blank fields"
        HorizontalAlignment="Center" 
        FontSize="32"
        Margin="100"
        />
        <StackPanel Orientation="Horizontal" Spacing="0" HorizontalAlignment="Left">
            <ItemsControl ItemsSource="{x:Bind QuestionElements}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding}" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <Button Background="Transparent"
                HorizontalAlignment="Center"
                BorderThickness="5"
                Padding="12"  
                Click="Send_Click" 
                Width="150" Margin="40">Send</Button>
    </StackPanel>

</UserControl>
