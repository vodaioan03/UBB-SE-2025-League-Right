<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Duo.Views.Pages.RoadmapMainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:Duo.Views.Components"
    xmlns:viewmodel="using:Duo.ViewModels.Roadmap"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Quiz page" 
                   Grid.Row="0"
                   HorizontalAlignment="Center" 
                   FontSize="32"/>

        <!-- <components:AssociationExercise Question="Connect the countries to their capital" FirstAnswersList="{x:Bind FirstAnswersList}" SecondAnswersList="{x:Bind SecondAnswersList}"/> -->
        <!-- <components:MultipleChoiceExercise Question="Test question" Answers="{x:Bind Answers}" /> -->
        <!-- <components:FillInTheBlanksExercise Question="Test test {} Test {}" /> -->

        <!-- <components:RoadmapSectionView Section="Countries" 
                                      Quizzes="{Binding Quizzes}" 
                                      Exam="{Binding Exam}" 
                                      OpenQuizPreviewCommand="{Binding OpenQuizPreviewCommand}" 
                                      DataContext="{Binding SectionViewModel}" 
                                      x:Name="RoadmapSectionView" /> -->

        <components:RoadmapSectionView DataContext="{Binding SectionViewModel}" 
                                      x:Name="RoadmapSectionView" />
        
        
        <components:RoadmapQuizPreview></components:RoadmapQuizPreview>
    </Grid>
</Page>