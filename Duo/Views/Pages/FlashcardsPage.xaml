<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Duo.Views.Pages.FlashcardsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Duo.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:Duo.Views.Components"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Flashcards" 
                   Grid.Row="0"
                   HorizontalAlignment="Center" 
                   FontSize="32"
                   Margin="0,20,0,20"/>

        <components:FlashcardExercise x:Name="FlashcardExercise" 
                                      Grid.Row="1" 
                                      ExerciseCompleted="FlashcardExercise_ExerciseCompleted" 
                                      ExerciseClosed="FlashcardExercise_ExerciseClosed"/>
                                      
        <!-- Navigation Buttons -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center" 
                    Margin="0,20,0,20">
            <Button x:Name="PreviousButton" 
                    Content="Previous Card" 
                    Margin="10,0" 
                    Padding="15,10" 
                    Click="PreviousButton_Click"/>
            <Button x:Name="NextButton" 
                    Content="Next Card" 
                    Margin="10,0" 
                    Padding="15,10" 
                    Click="NextButton_Click"/>
        </StackPanel>
    </Grid>
</Page> 